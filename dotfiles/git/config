[user]
    email = manabat.online@gmail.com
    name = louis-manabat
[commit]
    gpgsign = false
[init]
    defaultBranch = main
[alias]
    a = add
    aa = add .
    b = branch
    cl = clone
    cm = commit -m
    co = checkout
    l = log
    nb = "!git checkout -b $1 && git push -u origin $1"
    pa = "!f() { git add -A && git commit -m \"$1\" && git push; }; f"
    ph = push
    pl = pull
    rsh = reset --soft HEAD~1
    s = status
    us = restore --staged
    usa = restore --staged .
[credential]
	helper = store
