(($+commands[git])) && { alias g="git" }
(($+commands[docker])) && { alias d="docker" }
(($+commands[docker-compose])) && { alias dc="docker-compose" }
(($+commands[kubectl])) && { alias k3s="kubectl" }
(($+commands[terraform])) && { alias tf="terraform" }
(($+commands[python3])) && { alias py3="python3" }
